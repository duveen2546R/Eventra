services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eventra-backend
    env_file:
      - .env
    ports:
      - "${SERVER_PORT:-8080}:8080"
    restart: unless-stopped
